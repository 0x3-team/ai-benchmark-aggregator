You are a senior Python engineer. Repo: /srv/hermes/development/ai-benchmark-aggregator (ledger at ledger/, Python 3.11 venv at ledger/.venv).

TASK: Build a new ledger ingestion adapter for IMO-AnswerBench (imo_answerbench) and wire it into official_sources.yaml.

STEP 0 — DISCOVER the real CSV. The HF dataset google-deepmind/superhuman returned 401 on the public datasets-server (needs HF token or different path). Try, verify HTTP 200:
- HF datasets-server first-rows with token if HF_TOKEN set: https://datasets-server.huggingface.co/first-rows?dataset=google-deepmind%2Fsuperhuman&config=default&split=train
- or the raw CSV on the github.com/google-deepmind/superhuman repo (web_search "google-deepmind superhuman answerbench_v2.csv raw github")
- or if HF_TOKEN is available in env, use it as Bearer for the datasets-server rows API.
The CSV has columns like model, problem_id, correct (bool/0-1) — confirm actual header by fetching a sample. If no 200 source found, report blocker, do NOT fabricate.

ADAPTER: source_type = "imo_answerbench".
- fetch(source): httpx GET source.source_url with HF Authorization header if settings.hf_token present. Return SourceFetchResult.
- extract_claims: csv.DictReader over raw. Group rows by model_field (default "model"). For each model compute mean of the "correct"/"score" column (parser_config score_field default "correct") → accuracy. Emit one ResultClaimInput per model: score_raw=f"{acc:.4f}", metric_raw="accuracy_over_problems", score_numeric=acc, capture_method="imo_answerbench_parser", capture_confidence=0.9, capture_status="parser_verified", officialness_level=source.officialness_level.
- validate_claim: [ClaimValidationInput(validation_type="imo_csv_match", outcome="pass" if score_raw in text else "uncertain", validator="ImoAnswerBenchAdapter")].
- Imports: SourceAdapter (base), OfficialSource/ResultClaimInput/SourceFetchResult/ClaimValidationInput (boundary), try_parse_score (normalize), csv, io, httpx, get_settings.

REGISTER: add `from app.ingestion.adapters.imo_answerbench import ImoAnswerBenchAdapter` and `ADAPTERS["imo_answerbench"] = ImoAnswerBenchAdapter` in ledger/app/ingestion/adapters/__init__.py.

TEST: ledger/tests/test_imo_answerbench.py — sample CSV rows, assert aggregated accuracy claim per model. `cd ledger && source .venv/bin/activate && python -m pytest tests/test_imo_answerbench.py -q` → PASS.

VERIFY: `cd ledger && source .venv/bin/activate && python -c "from app.ingestion.adapters import get_adapter; print(type(get_adapter('imo_answerbench')).__name__)"`.

Do NOT edit official_sources.yaml. Commit: `git add ledger/app/ingestion/adapters/imo_answerbench.py ledger/app/ingestion/adapters/__init__.py ledger/tests/test_imo_answerbench.py && git commit -m "feat(ledger): add IMO-AnswerBench CSV ingestion adapter"`.
Report discovered URL, files changed, test result.
