You are a senior Python engineer. Repo: /srv/hermes/development/ai-benchmark-aggregator (ledger at ledger/, Python 3.11 venv at ledger/.venv).

TASK: Build a new ledger ingestion adapter for FrontierMath (frontiermath) and wire it into official_sources.yaml.

STEP 0 — DISCOVER the real FrontierMath leaderboard CSV. https://epoch.ai/api/v1/frontier-math/leaderboard.csv returned 404 (redirects to epoch.ai). Try, verify HTTP 200:
- https://epoch.ai/api/v1/frontier-math/leaderboard.csv (with -L follow)
- https://epoch.ai/frontier-math (page) and find the linked CSV
- web_search "epoch.ai frontier math leaderboard csv download raw url"
If the public endpoint requires auth/JS, report blocker and do NOT fabricate. Confirm the CSV header (model, score or pass_rate) by sampling.

ADAPTER: source_type = "frontiermath_epoch".
- fetch(source): httpx GET source.source_url (follow_redirects=True). Return SourceFetchResult.
- extract_claims: csv.DictReader over raw. model_field default "model", score_field default "score" (or "pass_rate" — match actual header). Emit one ResultClaimInput per row: score_numeric = float(score), metric_raw = score_field, capture_method="frontiermath_epoch_parser", capture_confidence=0.9, capture_status="parser_verified", officialness_level=source.officialness_level.
- validate_claim: [ClaimValidationInput(validation_type="fm_csv_match", outcome="pass" if score_raw in text else "uncertain", validator="FrontierMathEpochAdapter")].
- Imports: SourceAdapter (base), OfficialSource/ResultClaimInput/SourceFetchResult/ClaimValidationInput (boundary), try_parse_score (normalize), csv, io, httpx, get_settings.

REGISTER: add `from app.ingestion.adapters.frontiermath_epoch import FrontierMathEpochAdapter` and `ADAPTERS["frontiermath_epoch"] = FrontierMathEpochAdapter` in ledger/app/ingestion/adapters/__init__.py.

TEST: ledger/tests/test_frontiermath_epoch.py — sample CSV, assert claim per model. `cd ledger && source .venv/bin/activate && python -m pytest tests/test_frontiermath_epoch.py -q` → PASS.

VERIFY: `cd ledger && source .venv/bin/activate && python -c "from app.ingestion.adapters import get_adapter; print(type(get_adapter('frontiermath_epoch')).__name__)"`.

Do NOT edit official_sources.yaml. Commit: `git add ledger/app/ingestion/adapters/frontiermath_epoch.py ledger/app/ingestion/adapters/__init__.py ledger/tests/test_frontiermath_epoch.py && git commit -m "feat(ledger): add FrontierMath Epoch CSV ingestion adapter"`.
Report discovered URL, files changed, test result.
