---
name: better-ui
description: Use for interface polish: spacing, borders, shadows, icons, hover states, micro-interactions, and visual alignment.
---

# Better UI

Prefer optical alignment over arbitrary geometry. Use restrained state changes, consistent radius and shadow tokens, meaningful icons, clear focus, and motion that explains cause and effect. Fix the underlying component or token rather than adding one-off overrides.
