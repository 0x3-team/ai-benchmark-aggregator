# Model inventory checkpoint handover

**Date:** 2026-08-25
**Repository:** `0x3-team/ai-benchmark-aggregator`
**Branch:** `main`
**Checkpoint:** review-only model discovery and identity inventory
**Status:** historical checkpoint retained; generator amended after the real-only frontend cutover

## Purpose

This checkpoint started the model-list work before the real-only frontend
cutover. Its original bounded, source-hashed candidate inventory remains a
historical review receipt. The current generator now reads only the tracked
unavailable Official artifact for frontend state; it does not read a separate
model catalog. Neither the historical inventory nor a new rehearsal output is
an Official artifact, source certification, runtime input, or claims-ledger
input.

## Source counts

| Source | Count | Interpretation |
|---|---:|---|
| `src/data/official/export.unavailable.json` | 0 | Current immutable awaiting-publication frontend artifact |
| `models.yaml` + `models_frontier.yaml` + `models_hf_seed.yaml` | 1,186 rows / 1,186 unique IDs | Ledger registry candidates |
| OpenRouter API snapshot | 417 | Provider catalog entries at capture time |
| Hugging Face top-download snapshot | 100 | Bounded sample, not the full Hub |
| Normalized candidate keys | 1,571 | Current generator rehearsal after simple punctuation/case normalization |

The inventory intentionally keeps candidates unresolved. Simple normalization
is only a lead. It does not merge versions, providers, aliases, benchmark
systems, embeddings, adapters, or unrelated ML models.

## Hashes and inputs

- Historical v1 inventory: `docs/data/model-inventory-2026-08-25.json`
  - SHA-256: `ac557f1f1bc0cfcb91f74de2cc6927d9415aca9fe6f7989a548b08af07de103e`
- Current frontend containment input:
  `src/data/official/export.unavailable.json`
  - File SHA-256: `18a8eba3760d33690e57e1498a85ff5feb9ef00102b1b053856cb9d7dae095ad`
- OpenRouter snapshot:
  `docs/data/model-discovery-snapshots/2026-08-25/openrouter.json`
  - SHA-256: `94200dc1cbef54a3818614334b49abaf17877e8c24c52ab8042e32adec177b91`
- Hugging Face snapshot:
  `docs/data/model-discovery-snapshots/2026-08-25/huggingface-top100.json`
  - SHA-256: `94f6b8ba844499412d80eb304bb5839c16d3395d87c35be8a164509f7287d799`

The historical inventory was generated by an earlier version of
`scripts/build_model_inventory.py`. The current v2 generator validates the
tracked unavailable artifact's containment shape and zero counts before using
its empty `models` array. It refuses to overwrite an existing output file, so
generate into a new path for each discovery cycle or rehearsal.

## Runtime and trust boundary

- The full synthetic score and model catalogs have been removed from
  `src/data`.
- The tracked Official artifact remains unavailable and contains zero models,
  benchmarks, source snapshots, and scores.
- No candidate was written to the frontend runtime, the claims ledger, or an
  Official release.
- No source was certified and no claim was published.

The empty awaiting-publication product is intentional. It remains empty until
model identity and scope review produces a governed Official release; inventory
candidates are never a fallback dataset.

## Required next owner actions

1. Define the comparison scope: chat/reasoning, coding, multimodal, embedding,
   reranking, image/audio, benchmark systems, and test fixtures.
2. Review every candidate disposition: `confirmed`, `needs_review`,
   `deprecated`, `non_comparison_model`, or `synthetic_fixture`.
3. Build canonical identities with provider IDs, API IDs, Hugging Face IDs,
   OpenRouter IDs, aliases, version relationships, and evidence references.
4. Enrich confirmed identities with vendor, family, release date, context,
   parameters, modalities, weights, pricing, official URLs, and status.
5. Produce a reviewed model catalog and an append-only decision receipt.
6. Keep the frontend in its immutable empty awaiting-publication state until a
   separately governed Official artifact and release authorization are ready.

## Validation and resume commands

```sh
SOURCE_DATE_EPOCH=1787616000 ledger/.venv/bin/python scripts/test_build_model_inventory.py

tmpdir="$(mktemp -d)"
SOURCE_DATE_EPOCH=1787616000 ledger/.venv/bin/python scripts/build_model_inventory.py \
  docs/data/model-discovery-snapshots/2026-08-25/openrouter.json \
  docs/data/model-discovery-snapshots/2026-08-25/huggingface-top100.json \
  "$tmpdir/inventory-a.json"
SOURCE_DATE_EPOCH=1787616000 ledger/.venv/bin/python scripts/build_model_inventory.py \
  docs/data/model-discovery-snapshots/2026-08-25/openrouter.json \
  docs/data/model-discovery-snapshots/2026-08-25/huggingface-top100.json \
  "$tmpdir/inventory-b.json"
cmp "$tmpdir/inventory-a.json" "$tmpdir/inventory-b.json"
node -e 'const x=require(process.argv[1]); console.log(x.counts, x.syntheticDataRemoved, x.sources.frontendOfficialArtifact)' "$tmpdir/inventory-a.json"
rm -rf "$tmpdir"
```

The fixed `SOURCE_DATE_EPOCH` makes the two rehearsal outputs byte-identical;
without it, each new checkpoint records its actual generation time. Distinct
output paths remain mandatory because checkpoint files are immutable by design.

## Git and handover state

At checkpoint creation, local `main` was three commits ahead of `origin/main`
at `5eb3b35`, with a protected dirty worktree containing prior remediation,
security, discovery, frontend, migration, and test changes. Those existing
changes were preserved. This handover and the inventory are the only new
model-discovery outputs from this slice.

No push, deploy, database migration, source certification, or production
operation is part of this checkpoint.

## Post-checkpoint push receipt

The reviewed checkpoint was committed and pushed after local validation:

- Commit: `4bbc495` (`chore: checkpoint model inventory and remediation`)
- Remote: `origin/main`
- Push result: accepted by GitHub
- Deployment: none
- Database migration: none
- Source certification: none
- Official publication: none

GitHub reported three Dependabot vulnerabilities on the default branch (two
high and one moderate). CI/live acceptance remains separate from this push and
must be checked before release decisions.
